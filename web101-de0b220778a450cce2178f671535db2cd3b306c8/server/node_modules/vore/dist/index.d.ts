declare class Model<MC> {
    model: MC;
    constructor(model: MC);
}
declare type Models<MS> = {
    [K in keyof MS]: Model<MS[K]>;
};
export declare class Store<MS> {
    stores: Models<MS>;
    constructor(models: MS);
    get<K extends keyof MS>(key: K & string): MS[K];
}
export declare function makeAutoReactive(obj: any): any;
export {};
